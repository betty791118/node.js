<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>Member Page</h3>
    <div>歡迎登入, <%= name %></div>
    <div><%= email %></div>
    <hr/>
    <form method="POST" action="/postMessage">
        內容 <input type="text" name="content" />
        <button>送出</button>
    </form>
    <hr/>
    
    <% for(let i=0; i<blogs.length;i++){%>
        <div>
            <b><%= new Date(blogs[i].time) %></b>
            <%= blogs[i].content %>
        </div>

    <%} %>
    
  
    
    
    <a href = "/signout">登出</a>
</body>
</html>